import React from 'react';

const BlogHeader = (props) => {
    const {image, text, className} = props;
    return (
        <img src="/img/service/blogBack.png" className="w-100"   alt=""/>
    );
};

export default BlogHeader;